# function definition
def divide(num1, num2): # first line of a function is called the signature (interface)
                        # parameters: listed by commas, named
    return num1 / num2

print(divide(9, 2)) # function call uses arguments


# takes two parameters, strings.
# returns the concatenated first letter of each.
# eg: mr, withers => mw